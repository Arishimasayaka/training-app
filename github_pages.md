# 筋トレアプリ (PWA / GitHub Pages対応)

50歳前後の男性が健康的で引き締まった身体を維持・向上させるためのシンプルな筋トレ管理Webアプリケーションです。

## 主な機能
- **自動曜日取得**: 端末の日付から現在の曜日を自動判別（固定表示なし・リロード対応）。
- **曜日別メニュー自動切り替え**:
  - 月：胸
  - 火：背中 (ジャグジー・サウナ 20分表示)
  - 水：ボクシングレッスン (筋トレ非表示)
  - 木：肩・肩幅 (朝サウナ表示 / **ケーブルフライ必須導入**)
  - 金：休養 (筋トレ非表示)
  - 土：全身
  - 日：弱点部位
- **ストレッチタイマー**: 毎日5分間 (1分×5種目) の自動切替タイマー機能。
- **筋トレタスク管理**: 0.5kg単位の調整、10回×3セット管理、完了時の【完了済み】エリアへの自動下部移動。
- **種目変更機能**: タップ一つで別種目に変更し、完了状態をリセット。
- **データ保存**: `localStorage` を使用し、ブラウザを閉じても記録を維持。

---

## GitHub Pages での公開手順

### 構成（単一ファイル構成）
リポジトリのルート（直下）に `index.html` および `README.md` を配置して公開します。

### 公開手順
1. GitHub で新しいリポジトリ（例: `kintore-app`）を作成します。
2. 作成した `index.html` と `README.md` をリポジトリのルートにコミット＆プッシュ（Push）します。
3. リポジトリの **Settings** タブに移動します。
4. 左側のメニューから **Pages** を選択します。
5. **Build and deployment** の **Source** で `Deploy from a branch` を選択します。
6. **Branch** で `main`（または `master`）の `/ (root)` を選択し、**Save** を押します。
7. 数分後、発行されたURL（例: `https://<ユーザー名>.github.io/kintore-app/`）にアクセスすれば、スマートフォンでアプリとして利用可能になります。